version: 2

# This is a Go library with no binaries to release.
# GoReleaser is configured here for validation only.
# The actual release is handled by go-semantic-release.

before:
  hooks:
    - go mod tidy

# No builds - this is a library only

release:
  disable: true
