version: "2"
linters:
  default: all
  disable:
    - gomodguard
    - noinlineerr
    - paralleltest
    - wsl
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - depguard
          - goconst
          - varnamelen
      - path: thresholds.go
        linters:
          - mnd
formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    - golines
