---
name: Super Linter

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  build:
    name: Super Linter
    runs-on: ubuntu-24.04

    permissions:
      contents: read
      packages: read
      # To report GitHub Actions status checks
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0
          persist-credentials: false
      - name: Super-linter
        uses: super-linter/super-linter@46e338cb21f669ef26d328450eb2930db63bdad9
        env:
          VALIDATE_BIOME_FORMAT: false
          VALIDATE_JSCPD: false # FIXME: will remove when zizmor is updated to 1.23
          # To report GitHub Actions status checks
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
